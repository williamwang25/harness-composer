## JavaScript/TypeScript Policy

- Inspect `package.json` scripts before choosing a test command.
- Prefer targeted package tests when the repository layout makes that possible.
- Read generated bundles and lockfiles only when they are directly implicated.
